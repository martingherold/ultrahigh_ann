# Dataset splits

Store reproducible train, validation, and test split definitions here. Prefer
small text formats such as CSV or JSON containing stable observation IDs.

Record the random seed and split-generation procedure alongside each file.
